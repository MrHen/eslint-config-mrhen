/* eslint-disable no-unused-vars */

const STATUS_OKAY = 200;

let numbers = [
    0,
    1,
    2,
    3,
    4,
    5,
];
let number = 0;

const response = {
    status: STATUS_OKAY,
};

const status = STATUS_OKAY;
numbers = number[2];
number += 1;
