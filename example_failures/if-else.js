/* eslint-disable no-unused-vars */

// no-else-return error
const beta = function beta(foo) {
    if (foo === "foo") {
        return "foo";
    } else if (foo === "bar") {
        return "bar";
    } else {
        return "zaz";
    }
}
